{
  "name": "android-messages",
  "productName": "Android Messages",
  "version": "0.0.1",
  "description": "An electron app that wraps Android Messages For Web",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack:osx": "electron-packager . AndroidMessages --out=dist/osx --platform=darwin --arch=x64 --icon=assets/mac/AndroidMessages.icns",
    "pack:win64": "electron-packager . AndroidMessages --out=dist/win --platform=win32 --arch=x64 --icon=assets/win/AndroidMessages.ico",
    "build": "yarn run pack:osx && yarn run pack:win64"
  },
  "repository": "https://github.com/erwstout/android-messages",
  "author": "Eric Stout",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "electron": "^4.0.0"
  },
  "dependencies": {
    "electron-fetch": "^1.3.0"
  }
}
